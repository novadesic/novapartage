/**
 * Ancienne suite Keycloak — désactivée.
 * À remplacer par un parcours magic link (Mailpit) — voir cypress/README.md.
 */
describe.skip('Intégration auth (legacy Keycloak — désactivé)', () => {
  it('placeholder — réécrire pour magic link', () => {
    // Voir frontend/cypress/README.md
  })
})
